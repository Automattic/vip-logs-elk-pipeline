apiVersion: 1

datasources:
  - name: Elasticsearch
    type: elasticsearch
    access: proxy
    url: http://elasticsearch:9200
    jsonData:
      index: "wpvip-logs-*"
      timeField: "@timestamp"
      esVersion: 8
    isDefault: true